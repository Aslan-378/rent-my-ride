<h1>All vehicles (<%= Vehicle.count %>)</h1>

<% @vehicles.each do |vehicle| %>
  <ul>
    <li><%= vehicle.category %> | <%= vehicle.model %></li>
    <li>From £<%= vehicle.price %> per day</li>
    <li><%= link_to'See more', vehicle_path(vehicle) %></li>
  </ul>
<% end %>


<%= link_to"Back to home page", root_path, class: "btn btn-primary" %>
